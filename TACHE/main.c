#include <stdio.h>
#include <string.h>
#include"tache.h"

int main()
{
  
float tph,tpf;
TPHF("utilisateur.txt",&tph,&tpf);
 printf("\nle taux de participation des femmes est %f",tpf);









 /* float x;

  x=float TPE("utilisateur.txt");
 if(x==-1)
   printf("le fichier utilisateur ne contient aucun electeur");
 else 
  printf("le taux de participation des electeurs est de %f pourcent\n",x);*/
  
 return 0;
}
